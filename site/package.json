{
  "name": "@sdcorejs/utils-docs",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "validate:content": "node scripts/validate-content.mjs",
    "validate:build": "node scripts/validate-build.mjs",
    "validate": "npm run typecheck && npm run test && npm run validate:content && npm run build && npm run validate:build"
  },
  "devDependencies": {
    "jsdom": "^29.1.1",
    "typescript": "~5.7.2",
    "vite": "^8.0.16",
    "vitest": "^4.1.9"
  }
}
